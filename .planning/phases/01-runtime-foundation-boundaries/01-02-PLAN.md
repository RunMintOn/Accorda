---
phase: 01-runtime-foundation-boundaries
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/core/config.ts
  - src/provider/openaiClient.ts
  - src/runtime/defaultRunner.ts
  - test/config.test.ts
  - test/default-runner.test.ts
autonomous: true
requirements: [CONF-01, CONF-02, FLOW-02]
user_setup: []
must_haves:
  truths:
    - Provider responses are preserved as structured results rather than reduced to plain text at the provider boundary.
    - Configuration stays small and explicit while producing actionable diagnostics on missing or invalid provider settings.
    - Runtime receives provider metadata such as `usage`, `model`, and `finish_reason` when available, but tolerates missing fields from OpenAI-compatible backends.
  artifacts:
    - src/provider/openaiClient.ts returns a normalized provider result object.
    - src/runtime/defaultRunner.ts consumes normalized provider results and emits explicit runtime/system status on failure paths.
    - test/default-runner.test.ts verifies normalized result handling and failure diagnostics.
  key_links:
    - `src/core/config.ts` feeds the provider adapter with the explicit config surface only.
    - `src/runtime/defaultRunner.ts` passes normalized provider metadata forward for later CLI projection.
---

<objective>
Clean up configuration loading and the provider boundary so runtime orchestration can evolve without hidden coupling.

Purpose: Prevent Phase 1 from cementing a text-only provider seam that would block context usage, diagnostics, and future tool-calling work.
Output: Small explicit config loading, normalized provider result objects, and tests for config/provider diagnostics.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-runtime-foundation-boundaries/01-CONTEXT.md
@.planning/phases/01-runtime-foundation-boundaries/01-RESEARCH.md
@src/core/config.ts
@src/provider/openaiClient.ts
@src/runtime/defaultRunner.ts
@test/config.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Keep config explicit and layered without expanding the user surface</name>
  <files>src/core/config.ts, test/config.test.ts</files>
  <read_first>src/core/config.ts, test/config.test.ts, .planning/phases/01-runtime-foundation-boundaries/01-CONTEXT.md, .planning/REQUIREMENTS.md</read_first>
  <action>Refactor `src/core/config.ts` so the exported config shape is explicit and concrete: `provider: { baseURL, apiKey, model }` plus `workspaceRoot` as a derived internal field. The only required user inputs remain `CONTEXTA_BASE_URL`, `CONTEXTA_API_KEY`, and `CONTEXTA_MODEL`; do not introduce OAuth, multi-provider catalogs, or new required env vars. Update `test/config.test.ts` so it verifies exactly that nested provider shape and the derived workspace root.</action>
  <verify>npx vitest run test/config.test.ts</verify>
  <acceptance_criteria>
    - `src/core/config.ts` still requires only `CONTEXTA_BASE_URL`, `CONTEXTA_API_KEY`, and `CONTEXTA_MODEL` as user-supplied provider settings
    - `src/core/config.ts` exposes a config object whose provider-related fields are distinct from workspace/runtime-derived values
    - `test/config.test.ts` asserts the Phase 1 config surface without adding extra required env vars
  </acceptance_criteria>
  <done>Config loading remains small and explicit while the internal shape is ready for later layering.</done>
</task>

<task type="auto">
  <name>Task 2: Return normalized provider results instead of plain text</name>
  <files>src/provider/openaiClient.ts, src/runtime/defaultRunner.ts, test/default-runner.test.ts</files>
  <read_first>src/provider/openaiClient.ts, src/runtime/defaultRunner.ts, src/core/contracts.ts, .planning/phases/01-runtime-foundation-boundaries/01-CONTEXT.md, ../24claude-code/src/utils/tokens.ts</read_first>
  <action>Change `src/provider/openaiClient.ts` so the main chat-completion function returns a normalized object with this concrete shape: `text`, optional `usage`, optional `model`, optional `finishReason`, optional `toolCalls`, and optional `raw`. Then update `src/runtime/defaultRunner.ts` to consume that normalized object instead of a bare string, preserve the metadata for later CLI status work, and handle missing optional metadata gracefully for OpenAI-compatible providers. Add `test/default-runner.test.ts` to cover both a metadata-rich provider result and a provider result where `usage` is absent.</action>
  <verify>npx vitest run test/default-runner.test.ts</verify>
  <acceptance_criteria>
    - `src/provider/openaiClient.ts` no longer returns `Promise<string>`
    - `src/provider/openaiClient.ts` contains normalized fields for `text`, optional `usage`, optional `model`, optional `finishReason`, and optional `raw`
    - `test/default-runner.test.ts` contains one case with provider metadata and one case without `usage`
  </acceptance_criteria>
  <done>The provider seam preserves structured response data and runtime can tolerate metadata gaps from compatible backends.</done>
</task>

<task type="auto">
  <name>Task 3: Replace ambiguous fallback behavior with explicit diagnostics</name>
  <files>src/runtime/defaultRunner.ts, test/default-runner.test.ts</files>
  <read_first>src/runtime/defaultRunner.ts, test/default-runner.test.ts, src/ui/events/projectEvents.ts, .planning/phases/01-runtime-foundation-boundaries/01-CONTEXT.md</read_first>
  <action>Update `src/runtime/defaultRunner.ts` so config/provider failures surface through explicit `system_status` events with concrete payload keys `message`, `level`, `stage`, `reason`, and `source` instead of silently degrading into an `echo:` answer that looks like valid assistant behavior. Keep the CLI usable, but make failure modes observable and distinguishable from successful assistant output. Extend `test/default-runner.test.ts` to assert that failure paths produce explicit diagnostic status events instead of a silent text-only fallback.</action>
  <verify>npx vitest run test/default-runner.test.ts</verify>
  <acceptance_criteria>
    - `src/runtime/defaultRunner.ts` does not contain `echo: ${text}`
    - `src/runtime/defaultRunner.ts` emits explicit status/system information when provider or config loading fails
    - `test/default-runner.test.ts` asserts that a failure path is distinguishable from a successful assistant answer
  </acceptance_criteria>
  <done>Config and provider failures become explicit runtime-visible diagnostics rather than ambiguous fallback responses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- Provider responses cross from external APIs into runtime orchestration.
- Config values cross from process environment into provider initialization and runtime diagnostics.

## STRIDE Register
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | information_disclosure | config/provider diagnostics | mitigate | Surface explicit diagnostics without rendering secrets such as API keys, auth headers, or raw request bodies. Tests should verify observable failure status, not secret values. |
| T-02-02 | tampering | provider response normalization | mitigate | Normalize optional metadata fields and preserve raw payload separately so runtime logic does not trust missing or malformed provider data as if it were complete. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run test/config.test.ts test/default-runner.test.ts`
- [ ] `npx tsc --noEmit`
- [ ] `npm test`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Provider boundary is structured enough to support later context usage and tool metadata without rewriting the adapter
  </success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-foundation-boundaries/01-02-SUMMARY.md`
</output>
