---
phase: 01-runtime-foundation-boundaries
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/contracts.ts
  - src/store/runtimeState.ts
  - src/runtime/stageOne.ts
  - src/runtime/stageTwo.ts
  - src/runtime/engine.ts
  - src/ui/events/projectEvents.ts
  - test/runtime-engine.test.ts
autonomous: true
requirements: [FLOW-02, CONF-02]
user_setup: []
must_haves:
  truths:
    - Runtime state is represented by explicit stage/status contracts rather than the current `isLoading`/`inStageTwo` booleans alone.
    - The runtime engine emits or returns enough structured state information for the UI to explain what stage the turn is in and why it changed.
    - Event projection accepts the new runtime/system status shape without falling back to malformed warnings for valid events.
  artifacts:
    - src/core/contracts.ts defines explicit runtime stage/state payload types for Phase 1.
    - src/runtime/engine.ts uses those contracts in its turn result path.
    - test/runtime-engine.test.ts verifies explicit stage/status behavior.
  key_links:
    - Runtime engine result shape and event/status contracts are consumed by `src/ui/events/projectEvents.ts`.
    - `src/store/runtimeState.ts` matches the canonical runtime state contract from `src/core/contracts.ts`.
---

<objective>
Formalize the runtime state, event, and stage contracts that every later phase will build on.

Purpose: Remove ambiguity around who owns stage truth before tool loop, session, and permission work increase complexity.
Output: Explicit runtime contracts, aligned engine result types, and tests proving the contracts drive turn behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-runtime-foundation-boundaries/01-CONTEXT.md
@.planning/phases/01-runtime-foundation-boundaries/01-RESEARCH.md
@src/core/contracts.ts
@src/runtime/engine.ts
@src/runtime/stageOne.ts
@src/runtime/stageTwo.ts
@src/store/runtimeState.ts
@src/ui/events/projectEvents.ts
@test/runtime-engine.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace thin runtime booleans with explicit Phase 1 state contracts</name>
  <files>src/core/contracts.ts, src/store/runtimeState.ts, src/runtime/stageOne.ts, src/runtime/stageTwo.ts</files>
  <read_first>src/core/contracts.ts, src/store/runtimeState.ts, src/runtime/stageOne.ts, src/runtime/stageTwo.ts, .planning/phases/01-runtime-foundation-boundaries/01-CONTEXT.md</read_first>
  <action>Update `src/core/contracts.ts` so Phase 1 has explicit runtime types for `RuntimeStage` and a shared status payload shape that includes at least `stage`, `reason`, and optional metadata for `source`, `toolName`, and `errorSource`. Replace the current assumption that `RuntimeState` is primarily `isLoading` plus `inStageTwo`; the canonical state object should instead include `stage: RuntimeStage`, `reason: string`, `streamingText`, and `pendingPermissionRequest`. Update `src/store/runtimeState.ts`, `src/runtime/stageOne.ts`, and `src/runtime/stageTwo.ts` to use the shared contracts so stage-one and stage-two return types can carry structured state/status information rather than only ad hoc text/tool data.</action>
  <verify>npx vitest run test/runtime-engine.test.ts</verify>
  <acceptance_criteria>
    - `src/core/contracts.ts` contains `type RuntimeStage =` with `idle`, `routing`, `answering`, `executing`, `waiting_permission`, and `error`
    - `src/core/contracts.ts` no longer defines `RuntimeState` as only `isLoading`, `inStageTwo`, `streamingText`, and `pendingPermissionRequest`
    - `src/runtime/stageOne.ts` or `src/runtime/stageTwo.ts` references the shared runtime stage/state contracts from `src/core/contracts.ts`
  </acceptance_criteria>
  <done>Canonical runtime contracts exist and the old boolean-only state model is no longer the primary truth source.</done>
</task>

<task type="auto">
  <name>Task 2: Thread explicit runtime state through the engine turn result</name>
  <files>src/runtime/engine.ts, test/runtime-engine.test.ts</files>
  <read_first>src/runtime/engine.ts, test/runtime-engine.test.ts, src/core/contracts.ts, .planning/phases/01-runtime-foundation-boundaries/01-CONTEXT.md</read_first>
  <action>Refactor `src/runtime/engine.ts` so `runTurn()` returns a structured object that includes `stage`, `reason`, `finalText`, and any status/event records needed by the UI. The return shape must distinguish routing, direct answer, clarify/proceed-style control flow, stage-two entry, and error paths. Keep the phase boundary intact: do not implement the full tool loop, but make the engine result precise enough that later UI code can display stage and reason without guessing. Extend `test/runtime-engine.test.ts` to assert the explicit result shape for at least the proceed path and one non-executing path.</action>
  <verify>npx vitest run test/runtime-engine.test.ts</verify>
  <acceptance_criteria>
    - `src/runtime/engine.ts` contains a typed return object that includes runtime stage/status data
    - `test/runtime-engine.test.ts` asserts the proceed path does more than `returnedToStageOne`
    - `test/runtime-engine.test.ts` includes at least one assertion for a non-executing path such as answer or clarify
  </acceptance_criteria>
  <done>The engine exposes structured turn-state output instead of a binary “returned to stage one” result.</done>
</task>

<task type="auto">
  <name>Task 3: Align event projection with the new runtime/system status contract</name>
  <files>src/ui/events/projectEvents.ts, src/core/contracts.ts</files>
  <read_first>src/ui/events/projectEvents.ts, src/core/contracts.ts, src/ui/messages/types.ts, .planning/phases/01-runtime-foundation-boundaries/01-CONTEXT.md</read_first>
  <action>Update `src/ui/events/projectEvents.ts` so valid runtime/system status events with payload keys `message`, `level`, `stage`, `reason`, and optional `source` are projected cleanly into renderable messages instead of being treated as malformed payloads. Preserve warning behavior for genuinely malformed events, but ensure valid status payloads support the runtime stage and reason fields needed by the later CLI status plan.</action>
  <verify>npx vitest run test/message-list.test.tsx</verify>
  <acceptance_criteria>
    - `src/ui/events/projectEvents.ts` contains explicit handling for the updated `system_status` payload shape
    - `src/ui/events/projectEvents.ts` still returns warning messages for malformed payloads
    - `src/ui/events/projectEvents.ts` can project a valid runtime/system status event without the string `Malformed system_status event`
  </acceptance_criteria>
  <done>UI projection is compatible with the new runtime/system status contract and remains defensive against invalid payloads.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- User input enters runtime decisions through stage-one and engine orchestration.
- Runtime state and status events cross into UI rendering through event projection.

## STRIDE Register
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | tampering | runtime contracts | mitigate | Use shared runtime types in `src/core/contracts.ts` and assert them in engine tests so malformed or incomplete state cannot silently become valid stage truth. |
| T-01-02 | denial_of_service | event projection | mitigate | Keep malformed payload guards in `src/ui/events/projectEvents.ts` while expanding valid `system_status` handling so bad payloads degrade to warnings instead of crashes. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run test/runtime-engine.test.ts`
- [ ] `npx vitest run test/message-list.test.tsx`
- [ ] `npx tsc --noEmit`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Runtime stage truth is explicit enough for later UI projection without relying on `isLoading`
  </success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-foundation-boundaries/01-01-SUMMARY.md`
</output>
