---
phase: 01-runtime-foundation-boundaries
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - src/ui/App.tsx
  - src/ui/components/Header.tsx
  - src/ui/components/RuntimeStatus.tsx
  - src/ui/messages/MessageList.tsx
  - test/message-list.test.tsx
  - test/interactive-app.test.tsx
autonomous: true
requirements: [FLOW-02, CONF-01]
user_setup: []
must_haves:
  truths:
    - The CLI shows the current runtime stage from runtime-owned state rather than only a generic thinking spinner.
    - The CLI can show why the stage changed using runtime/system status data.
    - Context utilization is shown only when provider metadata exists, and missing usage does not produce invented numbers.
  artifacts:
    - src/ui/components/RuntimeStatus.tsx renders runtime stage and optional context information.
    - src/ui/App.tsx wires runtime state/status projection into the main CLI flow.
    - test/interactive-app.test.tsx and test/message-list.test.tsx verify visible runtime status behavior.
  key_links:
    - `src/ui/App.tsx` consumes the engine/provider-enriched turn data from earlier plans.
    - `src/ui/components/RuntimeStatus.tsx` uses normalized metadata from the runtime/provider boundary without reading secrets or raw payloads.
---

<objective>
Wire visible status/state projection into the CLI so the runtime becomes legible to the user.

Purpose: Deliver the user-facing payoff of Phase 1 by exposing runtime truth directly in the terminal.
Output: A runtime status component, App wiring based on runtime state instead of just `isLoading`, and tests covering visible status behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-runtime-foundation-boundaries/01-CONTEXT.md
@.planning/phases/01-runtime-foundation-boundaries/01-RESEARCH.md
@src/ui/App.tsx
@src/ui/components/Header.tsx
@src/ui/messages/MessageList.tsx
@src/ui/events/projectEvents.ts
@test/message-list.test.tsx
@test/interactive-app.test.tsx
@../24claude-code/src/components/StatusLine.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a dedicated runtime status projection component</name>
  <files>src/ui/components/RuntimeStatus.tsx, src/ui/components/Header.tsx</files>
  <read_first>src/ui/components/Header.tsx, src/ui/App.tsx, .planning/phases/01-runtime-foundation-boundaries/01-CONTEXT.md, ../24claude-code/src/components/StatusLine.tsx</read_first>
  <action>Create `src/ui/components/RuntimeStatus.tsx` to render the current runtime stage name directly (`idle`, `routing`, `answering`, `executing`, `waiting_permission`, `error`) plus a concise reason/source string derived from runtime/system status data. The component should accept a status object that contains `stage`, `reason`, optional `source`, and optional context-usage metadata. Keep the component visually plain; do not imitate Claude Code's full ornamentation. If provider metadata includes `usage` and a known `contextWindow`, render context utilization. If usage is absent, render either nothing or a clear unknown state, but never fabricate a percentage. Update `src/ui/components/Header.tsx` as needed to host the new component.</action>
  <verify>npx vitest run test/interactive-app.test.tsx test/message-list.test.tsx</verify>
  <acceptance_criteria>
    - `src/ui/components/RuntimeStatus.tsx` exists
    - `src/ui/components/RuntimeStatus.tsx` contains the literal stage names `routing`, `answering`, `executing`, `waiting_permission`, and `error`
    - `src/ui/components/RuntimeStatus.tsx` contains logic that does not require context usage to always be present
  </acceptance_criteria>
  <done>The CLI has a dedicated place to render runtime truth without relying on spinner-only feedback.</done>
</task>

<task type="auto">
  <name>Task 2: Replace App-level loading heuristics with runtime-driven status wiring</name>
  <files>src/ui/App.tsx, src/ui/messages/MessageList.tsx</files>
  <read_first>src/ui/App.tsx, src/ui/messages/MessageList.tsx, src/runtime/defaultRunner.ts, src/core/contracts.ts, .planning/phases/01-runtime-foundation-boundaries/01-CONTEXT.md</read_first>
  <action>Refactor `src/ui/App.tsx` so the CLI is driven by runtime state/status data from the turn result rather than relying primarily on local `isLoading` as the user-visible truth. Keep whatever temporary local state is still needed for input flow, but make runtime stage the thing that is rendered to the user. Update `src/ui/messages/MessageList.tsx` so any generic `● Thinking...` fallback no longer conflicts with explicit runtime status rendering.</action>
  <verify>npx vitest run test/interactive-app.test.tsx</verify>
  <acceptance_criteria>
    - `src/ui/App.tsx` renders `RuntimeStatus`
    - `src/ui/App.tsx` no longer relies on `isLoading` as the only visible turn-state signal
    - `src/ui/messages/MessageList.tsx` does not render `● Thinking...` as the sole runtime-status output when explicit status data exists
  </acceptance_criteria>
  <done>App rendering is driven by runtime truth instead of generic loading heuristics.</done>
</task>

<task type="auto">
  <name>Task 3: Update UI tests to lock the visible Phase 1 behavior</name>
  <files>test/message-list.test.tsx, test/interactive-app.test.tsx</files>
  <read_first>test/message-list.test.tsx, test/interactive-app.test.tsx, src/ui/App.tsx, src/ui/components/RuntimeStatus.tsx, .planning/phases/01-runtime-foundation-boundaries/01-CONTEXT.md</read_first>
  <action>Revise `test/message-list.test.tsx` and `test/interactive-app.test.tsx` so they assert visible runtime stage information, status reason visibility, and the absence of fabricated context percentage behavior. Include at least one test path where context utilization is absent and the UI remains stable without inventing a number.</action>
  <verify>npx vitest run test/message-list.test.tsx test/interactive-app.test.tsx</verify>
  <acceptance_criteria>
    - `test/interactive-app.test.tsx` contains an assertion for visible runtime stage text
    - `test/interactive-app.test.tsx` contains an assertion covering missing or unknown context usage behavior
    - `test/message-list.test.tsx` still verifies transcript rendering alongside the new status behavior
  </acceptance_criteria>
  <done>The visible Phase 1 runtime behavior is locked by automated UI tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- Runtime state and provider metadata cross into user-visible CLI output.

## STRIDE Register
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | information_disclosure | runtime status UI | mitigate | Only display normalized, non-secret metadata in `RuntimeStatus`; never render API keys, raw provider payloads, or auth headers. |
| T-03-02 | integrity | context utilization display | mitigate | Render context percentage only from real provider usage plus known context window; if usage is missing, show unknown or omit the field rather than inventing data. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run test/message-list.test.tsx test/interactive-app.test.tsx`
- [ ] `npx tsc --noEmit`
- [ ] `npm test`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- User can see runtime stage and reason changes in the CLI without the UI guessing hidden state
  </success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-foundation-boundaries/01-03-SUMMARY.md`
</output>
